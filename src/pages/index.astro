---
import Layout from "../layouts/Layout.astro";
import BookList from "../components/BookList.vue"
import {getBooks} from "../data/books";

const books = (await getBooks())?.map(book => book.title);

// Index page
---

<Layout>
  <BookList books={books} />
</Layout>